package Questions_GroupWiseQuarter;

public class Q04_OddNum {
    public static void main(String[] args) {
        /*
        Write a method called isOdd with an int parameter and call it number.
        The method needs to return a boolean. Check that number is > 0,
        if it is not return false. If number is odd return true, otherwise return false.
         */
    }
}
